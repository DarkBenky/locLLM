# locLLM model — Python dependencies (unpinned: installs latest compatible).
#
# CUDA machines: install a matching torch wheel FIRST, e.g.
#   nvidia-smi                    # e.g. "CUDA Version: 12.1" -> cu121
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r requirements.txt
#
# System packages NOT covered by pip (only for SAFIM execution scoring):
#   openjdk (Java), mono (C#), g++ (C++)

# Core (training + inference)
torch
sentencepiece

# Inference API (model/infrance.py)
fastapi
uvicorn
pydantic
requests

# Training launcher (model/main_big.py)
questionary
wandb

# SAFIM evaluation (model/eval.py, safim_helpers.py)
datasets
httpx
tqdm
# Only needed for SAFIM eval scoring (NOT for training/inference).
# Python 3.9+ required (py3.8 has no compatible wheels):
# tree-sitter
# tree-sitter-language-pack

# Optional (uncomment for 8-bit AdamW optimizer):
# bitsandbytes

